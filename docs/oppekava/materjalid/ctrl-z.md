# `Ctrl+Z`

`Ctrl+Z` peatab WSL/Linuxi terminalis kogu Claude Code'i protsessi ja viib tagasi käsureale.

See ei ole undo ega kustuta tehtud tööd.

Claude Code'i jätkamiseks käivita:

```bash
fg
```

Tegevuse katkestamiseks kasuta `Esc`, mitte `Ctrl+Z`.
